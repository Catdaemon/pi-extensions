# @catdaemon/pi-sidebar

Shared status-card sidebar helpers for Pi extensions.

This package does not register Pi tools or commands directly. It provides a shared global registry for top-right status cards so independently loaded Pi packages can stack cards consistently.
