name: Publish Sidebar

on:
  release:
    types: [published]

jobs:
  publish:
    if: github.repository == 'Catdaemon/pi-extensions' && startsWith(github.ref_name, 'pi-sidebar-v')
    runs-on: ubuntu-latest
    environment: npm-publish
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: pnpm
      - run: npm install -g npm@11.5.1 && npm --version
      - run: pnpm install --frozen-lockfile
      - name: Verify release tag matches package version
        run: |
          VERSION="pi-sidebar-v$(node -p "require('./packages/sidebar/package.json').version")"
          if [ "$GITHUB_REF_NAME" != "$VERSION" ]; then echo "Tag $GITHUB_REF_NAME does not match $VERSION" >&2; exit 1; fi
      - run: pnpm --filter @catdaemon/pi-sidebar run typecheck
      - run: pnpm --filter @catdaemon/pi-sidebar test
      - run: pnpm --filter @catdaemon/pi-sidebar run pack:dry-run
      - run: cd packages/sidebar && npm publish --provenance --access public
