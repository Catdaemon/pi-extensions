name: Publish cmux

on:
  release:
    types: [published]

jobs:
  publish:
    if: github.repository == 'Catdaemon/pi-extensions' && startsWith(github.ref_name, 'cmux-v')
    runs-on: ubuntu-latest
    environment: npm-publish
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/publish-package
        with:
          package-dir: packages/cmux
          package-name: "@catdaemon/pi-cmux"
          tag-prefix: cmux-v
